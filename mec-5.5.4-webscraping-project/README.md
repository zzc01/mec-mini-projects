# Use scrapy spider to crawl the quotes.toscrape.com quotes. 
- Use CSS selector 
- Use XPATH selector 
- Recursively follow the next link to scrape the pages 
- Store the quotes into JSON file 
- Load the json file data into sqlite3 database 
